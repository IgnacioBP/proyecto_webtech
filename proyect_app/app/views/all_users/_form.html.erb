<%= form_with(model: all_user) do |form| %>
  <% if all_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(all_user.errors.count, "error") %> prohibited this all_user from being saved:</h2>

      <ul>
        <% all_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
